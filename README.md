# TODO

why 8 seeds? saw in openai's default spec for evals, but seems it's misleading? they just ran each benchmark 8 times and reported mean? or pass@8? certainly not pass^8, but need to double check