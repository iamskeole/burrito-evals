# Tone/flow/readability summary — story.md (rung b)

## Region C (L239-L301)

- **4.4** (L245, "Effect"): The BFCL claim no longer reads as a flat "pattern reverses slightly" — it now states the reversal is fc-mode-dependent: with structured tool schemas (fc_model=1) high effort slips slightly below medium (76.1–76.7% → 74.3–75.9%, per plots.md Fig. 6.4 / report Finding 6), while with AST parsing (fc_model=0, the mode in the story's own pooled BFCL tables) the climb continues to high effort, which is why those tables show no reversal.
- **4.21** (L249, "Cost"): Replaced the irreproducible "70% → 98%" reasoning-to-output ratio with a vLLM-scoped version the story's own median-token tables and plots.md Figs. 6.13/6.14 support (AIME25: ~78% at low → ~98% at high; GPQA: ~94% → ~100%).
- **3.5** (L271): Reworded "This pattern translates across backends and resulting accuracies" to "The pattern holds across a wide range of accuracy levels, not just one" (same meaning, clear English).
- **2.9** (L287-289): Added first-use parenthetical glosses — MXFP4 (a 4-bit quantization format), GGUF (the file format llama.cpp uses for quantized models), safetensors (PyTorch's file format for model weights).
- **4.17** (L291): Made the BFCL version explicit and mappable: "the Big Function Calling Leaderboard, version v4 — the benchmark the report defines as BFCL" (maps the story/plots "BFCL_v4" naming to the report's benchmark definition, report.md L74).
- **1.6** (L301 closing note): Defined the relationship — a run is one question × seed × configuration (one model attempt at one benchmark item under one configuration and one of the eight random seeds) — and presented the 320K runs / 300K questions as two unit-level views of the same data instead of two unreconciled headline counts.
- **5.5** (L301 closing note): Replaced the unmappable "benchmark design flaws, template mismatches, and wire API differences" with the report's actual seven Finding-N titles (The Irrelevance Paradox, The Jinja Fix, Wire API Differences, Multi-Turn Base (Agentic Work), Preserved Thinking, Not All Reasoning is Created Equal, Python Tool Impact) and identified this story as Finding 6.
- **5.7** (new Methodology paragraph after the benchmarks line): Added the scoping note — this story's tables cover only the two burrito backends and center on AIME25/GPQA, with multi-turn and BFCL results where relevant; the wider cuts (all seven backends, all nine tests) live in the report and the 44-figure record in plots.md.

### Deferrals / notes (not applied in this region)

- **2.7** (fc_model undefined): first use is in the table headers at L163-227 (outside Region C); Region C now glosses fc_model=0/1 inline at their prose uses, but the headers themselves remain for the L1-238 owner.
- **3.6** ("--" cells vs the all-three-efforts bin rule): the table with the "--" cell is at L51 (outside Region C); the methodology's stated bin rule was left as-is (not on this region's assigned list).
- **4.14** (0% vs 0.1%/0.25% mismatch): per the findings file this is a report.md↔plots.md fix, not a story one — deferred to the report/plots owner.
- **4.16** (internal to story.md): "GPQA Diamond" (L291) vs the report's plain "GPQA" — observed while editing L291 but not on this region's assigned list; left unchanged.
- **Story-owned items whose first use lies in L1-238** (1.5, 2.8, 3.3, 4.2, 4.3 at L110, 4.5, 4.12, 5.1, 5.2): outside this region; the new 5.7 scoping note partially addresses the 4.5 story side (vanilla-vs-harness scoping).

## Region B (L87-L238)

- **4.3** (L110): Corrected the medium-effort plateau to match the story's own table and plots.md Fig. 6.1 / post — now "holds near 100% from 362 through 1,448 tokens — 100% at 362 and 724, 97.1% at 1,448" (the old "from 362 to 5,793" was contradicted by the table: 90.7% at 2,896, 84.3% at 5,793), followed by the actual decline path "90.7% at 2,896, 84.3% at 5,793, down to 30% at 46,341 and 0% at 92,682". All numbers from the adjacent table; mirrors the post's "362 through 1k" (the ~1,448 row is that same 1024–2048 bin).
- **4.12** (L108): Located the dangling low-effort crash endpoint per plots.md Fig. 6.1 ("collapses to 0% by 8k–16k") and the story's table: "crashes to 18.8% at 2,896 tokens, and to 0% by the 8k–16k bin (~11,585)" — the post's version ("19% at 2,896 … 0% by the 8k-16k bin") rendered with the story's exact table values.
- **2.8** (L141): Defined "multi-turn tasks" at first use — runs of multi_turn_base, the agentic test in which the model must make a sequence of tool calls and keep the conversation on track across multiple turns (per report.md L98/L254 and plots.md Fig. 6.2) — so the term is introduced before its later uses (Fig. 4 caption, "Multi-turn base" tables).
- **2.7** (L163 first table header, plus the other seven fc_model=0 headers in the section): Added a prose definition of fc_model before the first table header (fc = function calling; 0 = mode without structured tool schemas, tool calls parsed from plain text output via AST; 1 = mode with structured tool schemas — report.md L74). No table header or cell was changed; the single first-use definition covers all eight headers, and it lands ahead of Region C's L247 usage ("fc_model=0, the mode in the tables above").
- **4.14** (L112): Resolved without an edit — the story's "high effort stays above 90% from 1,448 to 46,341" is consistent with both its own table (minimum in that range: 90.6% at 46,341) and plots.md Fig. 6.1 ("stays above ~88% all the way to 32k–64k", which 90.6% satisfies). The story side is the more precise of the two; nothing to correct here.

### Deferrals (not applied — other documents)

- **4.14 (cross-document remainder)**: the post's "above about 88%" (post.md L66) vs the story's "above 90%" is a precision drift, not a contradiction — the actual floor is 90.6% at the 32k–64k bin, so both phrasings are true; the post is frozen. The tone-post's "(0% vs 0.1% mismatch)" label itself maps to the report↔plots cell discrepancy in findings 4.14 proper (report 0.2% vs plots 0.25% for default-jinja multi-turn fc=0) — both documents frozen, no story-side action.

## Region A (L1-L86)

- **5.2** (new preamble paragraph, L5, between the subtitle and the rule): Added the recap ramp for post-readers — gpt-oss-20b is OpenAI's open-weight 20B model whose infrastructure, not the model, was broken (broken prompt templates, mismatched wire APIs, ignored tool schemas on the vanilla llama.cpp/vLLM backends, per the post); burrito is the harness built to fix that and every number in the story comes from the burrito backends; "reasoning effort" is glossed as the model's low/medium/high "how hard to think" setting that controls how much thinking a run does before answering.
- **5.1** (same ramp): Added the forward pointer — of the post's four evaluation dimensions (reasoning benchmarks, multi-turn workflows, preserved thinking, effort levels), this story covers the AIME25/GPQA/multi-turn/BFCL runs and the effort dimension, while preserved thinking is covered in the full report: report.md, Finding 5: Preserved Thinking (exact heading per report.md L297; report.md not edited).
- **3.3** (L13, "The Assumption"): "rejects this model entirely" → "rejects that working model entirely" — the ambiguous "this model" (gpt-oss-20b in the post's usage) now unambiguously refers back to the section's working hypothesis.
- **4.2** (L17 "The Assumption" and L21 "The Experiment"): Corrected the dataset count to match what the story's tables actually cover — three named benchmarks (AIME25, GPQA, Big Function Calling Leaderboard, each with its first-use gloss) plus the multi-turn task suite, referenced generically (the multi-turn tables below the region were not touched). Phrased as "three benchmarks … plus the multi-turn task suite" so it stays consistent with the frozen Methodology line "Three benchmarks …" (now L297), which is immediately followed by its scoping note naming multi-turn.
- **1.5** (L21 "The Experiment"): "Temperature 1.0, the producer recommended setting" → "Temperature 1.0, the documented default recommended by the producer, OpenAI" — producer named, default status stated.
- **4.5** (L21-L23 "The Experiment"): Backend scope made explicit — "Two inference backends: burrito with llama.cpp and burrito with vLLM — the harness's backends, not the vanilla versions, which were broken on this model," and the following sentence now reads "The two burrito backends … identical on burrito's llama.cpp and vLLM backends." Matches plots.md Fig. 6.15 ("burrito backends, both tool modes") and the post's vanilla-broken setup, so a post-reader can no longer mistake the headline curves for vanilla-backend results.
- **3.6** (L57, after the AIME25 backend-split table): Added a table-level explanation of the "--" cell — it marks a cell with no data (no high-effort vLLM runs landed in that reasoning-token bin, so no accuracy is reported), while the pooled table above pools across backends and therefore still carries a value (100.0%) for that bin. Cross-checked against the Methodology bin rule (the "all three effort levels" rule governs pooled bins) and plots.md Fig. 6.15; the pooled 100.0% is consistent with the llama-side 100.0% plus an empty vLLM cell. No table cells, headers, or numbers were modified.

### Deferrals (not applied in this region)

- **3.6 (deeper cause)**: *why* no high-effort vLLM runs land in the ~1,448 bin specifically (the per-tool-mode run distribution behind the pooled vs split difference) is stated in neither the tables nor plots.md, so the explanation stops at the table level rather than asserting a cause.
- **4.2 (remainder)**: the Methodology's "Three benchmarks … GPQA Diamond …" line (L297-L299, outside this region) was left as-is — it is consistent with the L17/L21 fixes via its adjacent scoping note, and no other document owns a conflicting count.
