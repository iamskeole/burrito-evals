# Tone/flow/readability summary — plots.md (final polish)

Scope: additions-only on `report/plots.md`; the sole wording change to existing text is the approved "15 points" correction in the Fig. 6.8 caption. Baseline md5 262949813e1d57f57d2833039941387c (verified pre-edit); post-edit file is 318 lines (313 + 5 new). Verified by diff against `.polish-baseline/plots.md`: all pre-existing lines byte-identical except L265.

## 6th-manager final-polish changes

- L7 (new, Abstract): one sentence with the 3.49B headline composition from `.polish-composition.md` — ~3.12B input, ~367M output, of which ~154M recorded reasoning/thinking and the remaining ~214M the visible answer, phrased as the remainder of output minus recorded reasoning because the per-run thinking/visible split is recorded for only 28,661 of the 320,192 runs (deferral 5.3).
- L8 (new, Abstract): blank separator line after the added composition sentence.
- L41 (new, end of Abstract): two-sentence terminology note — a unit = one benchmark item (task/problem/question); a run (one row) = one model's attempt at one unit under one configuration and one seed; a trajectory = a multi-turn unit's attempt across its turns; output tokens = thinking/reasoning + visible answer (output = the whole) (deferrals 1.8, 2.5).
- L42 (new, end of Abstract): blank separator line before the `## Phase 1` heading.
- L269 (CHANGED; was L265, the Fig. 6.8 caption — the only existing-text change): "15 points" correction per deferral 4.9 — "(83.3% at 6,738 tokens)" → "(83.3% at 6,738 tokens, AIME25-only)" and "beat schema-free medium" → "beat schema-free medium (the AIME25+GPQA pool)"; no number changed, and the distinct "15.2 points on fc_model=0 (53.1 → 68.3%)" claim on the same line left byte-identical.
- L270 (new, Fig. 6.8 caption note, blockquote line immediately after L269): approved caveat — the figure compares per-seed pass^8 at matched reasoning length (y) against pooled accuracy across 8 seeds (x), and the fc_model=1 lines are AIME25-only (deferral 4.9).
