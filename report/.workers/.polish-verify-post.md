# VERIFIER-POST verdict — post.md polish (Qwen de-dupe + TPS wording)

Date of verification: 2026-08-21 (after 14:10, following the EDITOR-POST run).
Files checked:
- (1) `report/post.md` — 152 lines
- (2) `report/.polish-baseline/post.md` — 162 lines (pre-edit snapshot, taken 13:43:39)
- (3) `report/.polish-tone-post-summary.md` — editor's change list

---

## (a) QWEN DEDUPE — PASS

- Exactly one Qwen section remains in (1): `grep -c "## Qwen can't slop my ass"` → baseline = **2**, post.md = **1**. Same for the section's paragraphs: "People love to say LLMs are slop machines" → 2 vs 1; "I just co-authored a world-class" → 2 vs 1.
- The surviving section in (1) is **byte-identical** to the first/original copy in (2):
  - (2) first copy = L126–L132 (`## Qwen can't slop my ass` through "…One GPU is enough.").
  - (1) surviving section = L126–L132 (same line numbers, since the TPS edit was in-place).
  - `cmp <(sed -n '126,132p' baseline) <(sed -n '126,132p' post.md)` → exit 0, no differences. This includes the preserved trailing spaces on the two Qwen paragraph lines (L128, L130 end in ". ").
- The duplicated second copy from (2) (baseline L136–L142, itself byte-identical to the first copy per `cmp <(sed -n '126,132p') <(sed -n '136,142p')` → exit 0) is **absent from (1)**: the section appears exactly once (counts above), and the unified diff shows the block removed.
- Post-edit structure is clean: (1) L132 last Qwen line, L133 blank, L134 `---`, L135 blank, L136 `## Credits` — a single separator between the Qwen section and Credits.

## (b) TPS WORDING — PASS

- New phrasing present in (1): line 120 — "…It flies at 150-200 TPS (tokens per second) on consumer hardware. It is not a great thinker, but it is an incredible executor." (`grep -n '150-200 TPS' post.md` → exactly 1 hit, line 120.)
- Old phrasing absent: `grep -c '200-300 TPS'` → 0; en-dash variant `grep -c '200–300 TPS'` (U+2013) → 0.
- Dash codepoint verified with `od`: the sequence between the digits in (1) is `31 35 30 2d 32 30 30 0a` → the dash is **U+002D hyphen-minus** (0x2d), i.e. the original byte was preserved and only the digits changed — acceptable/expected per the spec, and consistent with the document's range style ("128-256", "8k-16k", "2k-4k" all use hyphens).
- Readability: the sentence is unchanged except for the digits — `cmp` of baseline L120 vs (1) L120 after restoring "150-200"→"200-300" → exit 0, proving the digits are the only change on that line. No double spaces on line 120 (`grep -c '  '` → 0). Surrounding context reads naturally; no broken grammar.

## (c) LINE-LEVEL DIFF (1 vs 2) — PASS

`diff -u .polish-baseline/post.md post.md` (exit 1) shows **exactly two hunks**, nothing else:

1. Hunk `@@ -117,22 +117,12 @@`, change at old L120: `200-300 TPS` → `150-200 TPS` (digits only; byte-proof in (b)).
2. Same hunk, a 10-line deletion. Note on alignment: because the two Qwen copies are byte-identical, the diff LCS aligned the removal to the *first* copy (baseline L126–L135: heading, 3 paragraphs, blank, redundant `---`, blank) and kept the second copy (baseline L136–L142) in place — this matches the editor's own note in (3) ("diff aligned the removal to the first identical copy; the surviving content is the kept block — verified byte-for-byte"). Independently verified here: the surviving block is byte-identical to the first (original) copy (check (a)), and the editor's described removal (second copy L136–L142 + redundant separator L134–L135 + blank L143 = 10 lines) is the same 10-line delta. Either way the final file is identical.
   - Deleted lines (per diff, baseline numbering): L126 `## Qwen can't slop my ass`, L127 blank, L128 "People love…", L129 blank, L130 "I just co-authored…", L131 blank, L132 "The prose you are reading…", L133 blank, L134 `---`, L135 blank — 10 lines, exactly the duplicated block + redundant separator/blank lines as listed in (3).
- Line counts: 162 → 152 = exactly 10 lines removed. ✓
- Every other baseline line appears verbatim, whole, and in order in (1) (151 unchanged lines + 1 replaced line). No other modifications, no reflow, no whitespace changes — the diff has no further hunks.

## (d) FILESYSTEM — PASS

Snapshot time in `.polish-baseline/.snapshot-time`: `2026-08-21 13:43:39`.
`find /home/p/code/local/burrito-evals -type f -newermt '2026-08-21 13:43:39'` returns exactly 14 files, all within the allowed newer set:

| File | mtime | Allowed as |
|---|---|---|
| report/.polish-baseline/plots.md | 13:43:39.856 | .polish-baseline/ |
| report/.polish-baseline/post.md | 13:43:39.856 | .polish-baseline/ |
| report/.polish-baseline/report.md | 13:43:39.856 | .polish-baseline/ |
| report/.polish-baseline/story.md | 13:43:39.856 | .polish-baseline/ |
| report/.polish-baseline/.snapshot-time | 13:43:39.857 | .polish-baseline/ |
| report/.polish-composition.py | 13:45:22 | explicitly listed scratch |
| report/.polish-composition.md | 13:45:27 | .polish-* |
| report/.polish-locator.md | 13:48:00 | .polish-* |
| report/plots.md | 13:55:15 | approved STEP 1 edit |
| report/.polish-tone-plots-summary.md | 13:55:41 | .polish-* |
| report/.polish-verify-plots.md | 14:04:33 | .polish-* |
| report/post.md | 14:08:15 | this step |
| report/.polish-tone-post-summary.md | 14:09:25 | .polish-* |
| report/.polish-state.md | 14:10:15 | .polish-* |

No other file in the tree (including `report/report.md`, `report/story.md`, `eval_report.ipynb`, and all `.git` internals) is newer than the snapshot time. No disallowed writes.

---

## Verdict summary

- (a) QWEN DEDUPE: **PASS**
- (b) TPS WORDING: **PASS**
- (c) LINE-LEVEL DIFF: **PASS**
- (d) FILESYSTEM: **PASS**

Issues: **0**

**POST GATE: PASS**
