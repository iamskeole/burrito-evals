# VERIFIER-STORY verdict — story.md polish (L295 + L309)

Scope verified: human-approved edits (1) L295 safetensors attribution, (2) L309 "300K questions / 320K runs" → plots vocabulary (320,192 = evaluations; ~300K kept; no new claims/numbers). Read-only verification; files diffed against `.polish-baseline/story.md`.

## Check (a) — both fixes present, correctly scoped, natural wording: PASS
- **L295**: baseline `...vLLM (safetensors — PyTorch's file format for model weights — in the same MXFP4 quantization)...` → live `...vLLM (safetensors — Hugging Face's file format for PyTorch tensors — in the same MXFP4 quantization)...`. Attribution corrected exactly as approved; surrounding sentence ("Two inference backends: llama.cpp (GGUF weights — ...) and vLLM (...) Both backends produce identical accuracy curves, confirming the pattern is backend-independent.") intact. Grammar clean, no dangling references.
- **L309**: baseline `...covering 320K runs, 300K questions, and 1,062 GPU hours of compute. A run is one question × seed × configuration — ... — so the two headline counts are two views of the same data: 300K questions (the benchmark items) and 320K runs (the individual question × seed × configuration attempts that evaluate them).` → live `...covering 320K evaluations, 300K units (benchmark items), and 1,062 GPU hours of compute. An evaluation is one question × seed × configuration — ... — so the two headline counts are two views of the same data: 300K units (benchmark items) and 320K evaluations (the individual question × seed × configuration attempts that evaluate them).` Defined term ("An evaluation is") now matches the headline counts; "them" correctly resolves to units (benchmark items). Reads naturally; the "evaluation covering … evaluations" echo is mildly repetitive but matches the approved change list verbatim.

## Check (b) — line-level diff vs baseline: PASS
- `diff -u .polish-baseline/story.md story.md`: exactly **2 hunks** — `@@ -292,7 +292,7 @@` (L295 single-line replacement) and `@@ -306,4 +306,4 @@` (L309 single-line replacement). No other line differs.
- `wc -l`: baseline **309**, live **309** — line count unchanged; no duplicated or truncated blocks (each hunk is a 1-for-1 line replacement; all surrounding lines byte-identical).
- Both changed lines fall inside the editor's owned region L280–L309.

## Check (c) — wording alignment: PASS
- L309 now describes the 320K (320,192) figure as **evaluations** in both places: the headline count ("320K evaluations") and the parenthetical ("320K evaluations (the individual question × seed × configuration attempts that evaluate them)"), with the definition sentence switched to "An evaluation is one question × seed × configuration".
- "300K" kept, relabeled **units (benchmark items)** — matches the plots vocabulary note ("a unit = one benchmark item").
- **No new numbers**: numeric tokens in L309 are identical to baseline (320K, 300K, 1,062, eight). **No new claims**: sentence structure and content otherwise byte-identical to baseline.
- Note (non-issue): other occurrences of the full figure — L13/L293 "320,192 evaluation runs", L21 "320,192 total runs" — are unchanged baseline text (outside the two approved edits); "run" remains the plots-defined term (one model's attempt at one unit under one configuration and one seed), so no misalignment is introduced.

## Check (d) — mtime sweep (metadata only): PASS
- Reference: `.polish-baseline/` dir mtime = **1787309019.857633629** (= `.snapshot-time`). All four baseline content files at 1787309019.856633632 — strictly older than the dir mtime.
- Files under `burrito-evals/` with mtime newer than the snapshot: exactly **12**, all in the approved set:
  - `report/story.md` (1787311527.966) — the file under verification
  - `report/plots.md` (1787309715.856), `report/post.md` (1787310495.829)
  - `report/.polish-*` scratch: `.polish-composition.py` (1787309122.535), `.polish-composition.md` (1787309127.206), `.polish-locator.md` (1787309280.208), `.polish-tone-plots-summary.md` (1787309741.299), `.polish-verify-plots.md` (1787310273.441), `.polish-tone-post-summary.md` (1787310565.465), `.polish-verify-post.md` (1787310797.719), `.polish-state.md` (1787310854.165), `.polish-tone-story-summary.md` (1787311540.310)
- **Violators: none.**

## VERDICT
| Check | Result |
|---|---|
| (a) fixes present / scoped / natural | PASS |
| (b) diff = exactly L295 + L309, 309 lines, no dup/truncation | PASS |
| (c) 320K = evaluations; 300K kept; no new claims/numbers | PASS |
| (d) no mtime violators beyond approved set | PASS |

STORY GATE: PASS — both approved edits verified; diff limited to L295+L309; 309 lines intact; mtime sweep clean — 0 issues.
