# Fresh-Reader Findings (post.md → story.md → report.md → plots.md)

Reader passed: a fresh reader with zero prior context, reading the four files in that order.
Category 4 note: the category is "inconsistencies between docs"; a small number of entries are contradictions *within* a single file that the same reader hits — each is explicitly tagged `(internal to <file>)` so the count stays transparent.

## 1. Assumed context

1. **post.md — title (line 1) / "The story" (line 22):** "batch size 1" (title) and "Batch size 1" (line 22) are never defined in any of the four documents, so a fresh reader doesn't know what a batch is or why 1 is worth headlining.
2. **post.md — "The story" (line 22):** "8 seeds" is used as a headline reliability claim but no document ever explains what a seed is; the post is the broadest-audience document in the set.
3. **post.md — "The worker bee" (line 118):** "It flies at 200-300 TPS" — TPS (tokens per second) is undefined and the number appears nowhere in the other three documents, so a fresh reader has no way to place or check it.
4. **post.md — "The worker bee" (line 120) / "Credits" (line 148):** "unsloth--Qwen3.6-27B-UD-Q4_K_XL.gguf" is used as a co-author's name without any explanation of what a .gguf filename, the "unsloth" prefix, or Q4_K_XL mean, so a fresh reader can't tell what kind of co-author this is or that it is a quantized model file.
5. **story.md — "The Experiment" (line 19):** "Temperature 1.0, the producer recommended setting" — no document names the producer (OpenAI) or cites where this recommendation lives, so a fresh reader doesn't know who the producer is or that the setting is the model's documented default.
6. **story.md — closing note (line 301):** "320K runs, 300K questions" — "questions" appears only here and its relationship to the 320,192 "runs" (runs = question × seed × configuration?) is never defined in any document, so a fresh reader cannot reconcile the two headline counts.
7. **report.md — "Backends" (lines 106-116):** the report presents "seven backend configurations are compared" as one fixed comparison set, but the underlying phases use different subsets (plots.md phase 1 title: "burrito-pt excluded"; Fig. 6.6: "five non-burrito-pt backends"), and the report never says which backends back each finding — a fresh reader assumes all seven are in every table.
8. **all four documents (e.g. post.md line 1 vs line 92; plots.md line 69):** the unit of analysis is variously called "evals" (post title), "runs" (post line 22, story, report), "rows" (post line 92, report line 76), "units" (plots captions, e.g. line 69), "tasks" (plots Figs. 5.x), "problems" (plots Figs. 2.x/4.x), and "trajectories" (plots Fig. 5.7), with no document defining how these relate — a fresh reader moving between documents cannot tell whether a "unit" equals a "row" or a "run".

## 2. Jargon used before introduction

1. **post.md — "The story" (line 11):** "The wire APIs were mismatched" — "wire API" is never defined in the post; it is only explained later in report.md Finding 3, so the post's first audience meets it cold.
2. **post.md — "The story" (line 24):** "especially on the fc=1 path" — fc_model/fc=1 is defined nowhere in the post (only in report.md lines 74/125), so a post-reader has no idea what fc=1 is.
3. **post.md — "The story" (line 22):** "preserved thinking" is listed as one of the four eval dimensions with no explanation in the post or the story; it is only defined in report.md Finding 5.
4. **post.md — "Finding 2" (line 68) / story.md — subtitle (line 3):** "test-time compute" is a term of art that no document ever defines (it appears in the post's closing takeaway and the story's subtitle).
5. **post.md — "Finding 1" (line 38) / "The tradeoff landscape" (line 80):** "reasoning tokens" (vs "output tokens") is never defined in any of the four documents — a fresh reader cannot tell whether reasoning tokens are a subset of output tokens or a separate count, which the entire reasoning story depends on.
6. **post.md — "Finding 1" (lines 42, 52):** "AIME25" and "GPQA" are benchmark names used without expansion in the post (expansion only in story.md line 19 and report.md line 74), so a fresh post-reader doesn't know what either benchmark is.
7. **story.md — "Cross-Benchmark Consistency" (line 163 and the seven other table headers):** "fc_model=0" appears in eight table headers of the file the post promises as a "standalone deep-dive," but story.md never defines fc_model at all (only report.md line 74 does).
8. **story.md — "The Degradation Curve" (line 141):** "multi-turn tasks" appears here without introduction — the story's own "The Experiment" section (line 19) lists three benchmarks that do not include multi-turn, so a story-reader meets the term out of nowhere.
9. **story.md — "Methodology" (lines 287-289):** "MXFP4," "GGUF weights," and "safetensors" are undefined format/quantization terms (the post uses MXFP4 at line 22 too), so a fresh reader at the methodology step doesn't know what any of them are.
10. **report.md — "Finding 1: The Data" (line 149):** "the phase's outlier error column (16.7–99.3% of units across tasks with fc_model=0)" — "phase," "column," and "units" are plots.md vocabulary used in the report with no introduction (the phase structure is only explained in plots.md lines 9-15).
11. **report.md — "Finding 1: The Data" (line 149):** "once the FC model is enabled" — a new term that re-casts fc_model=0/1 (a mode) as "the FC model" as if it were a model; the phrase appears nowhere else and a fresh reader will wonder what this model is.
12. **report.md — "The Problem" (line 53):** "implements function calling through grammar constraints" — the mechanism (constrained decoding) is never explained; a fresh reader accepts "grammar constraints" as a black box that "biases model output."

## 3. Flow breaks

1. **post.md — lines 124-140:** the "Qwen can't slop my ass" section appears twice, verbatim, back-to-back (lines 124-130 and 134-140); a fresh reader will assume a paste error and the duplicate interrupts the credits.
2. **post.md — "The worker bee" (line 120) → "Credits" (line 148):** the first-person narrator switches between the human author ("I spent a year fixing gpt-oss's tool calling," line 120) and the LLM co-author ("I executed the data queries, wrote the charts, and drafted the prose," line 148, where the human is named as the other co-author) with no cue marking the switch — a fresh reader cannot tell who "I" is in the credits.
3. **story.md — "The Assumption" (line 11):** "The data from 320,192 evaluation runs rejects this model entirely" — a reader arriving from the post, where "the model" consistently means gpt-oss-20b, will read "this model" as the model under evaluation rather than the working hypothesis of line 9; the ambiguity lands on the thesis sentence.
4. **post.md — "Finding 1" (lines 38-42) / story.md — "Methodology" (line 293):** the specific bin values (~181, ~1,448, ~2,896…) are used from the post's first table, but the binning method ("doubling bins") is defined only in the story's trailing Methodology — the post gives no method at all, so a fresh reader meets the seemingly arbitrary numbers before any explanation.
5. **story.md — "Transferable Lessons" (line 271) / report.md — Finding 6 (line 414):** "This pattern translates across backends and resulting accuracies" — the same verbatim phrase in both documents is unclear English; a fresh reader stumbles on what "translates across … accuracies" is supposed to claim.
6. **story.md — "AIME25 at matched reasoning length" (line 51):** the backend-split table has an unexplained "--" cell (High/vLLM at ~1,448 tokens) while the pooled table beside it reports High = 100.0% at the same budget; the stated inclusion rule (line 293, "only bins containing all three effort levels") does not obviously cover a per-backend missing cell.
7. **report.md — Findings 1-4 (lines 161-279):** the same image files are embedded as different numbered figures with different captions — phase_1-f01-mean_correct.png is "Fig. 1," "Fig. 3," and "Fig. 5," and phase_1-f05-is_error.png is "Fig. 2" and "Fig. 4" — so a fresh reader sees identical charts up to five times and cannot tell which region of the chart each caption refers to.
8. **plots.md — line 1:** the file opens with the post's title verbatim ("Don't trust me bro: 3.49B tokens, …") before the "## Abstract" on line 3; a reader opening plots.md cannot tell what kind of document they have landed in (a Reddit post? a figure catalog?) until line 5.
9. **report.md — "Finding 4: Failures Concentrate at Turn 0" (line 270):** the paragraph packs three different measurements (per-backend turn-0 clear rates, "639 of 1,600 trajectories even reach turn 0," and 0.9–7.1% survival through turn 6) in a single dense block without separating them, so a fresh reader loses the thread of what is being measured.

## 4. Inconsistencies between docs

1. **post.md line 95 / story.md line 301 / report.md TOC (lines 8-14):** the same seven findings are named three different ways — post's "the reasoning story" is the report's Finding 6 "Not All Reasoning is Created Equal" (= the story's own title), post's "native tool utility" is the report's "Python Tool Impact," and story's "benchmark design flaws" is the report's "The Irrelevance Paradox" — so a reader looking for a post-named section in the report will not find it under that name.
2. **story.md lines 15/19 (with line 179) / report.md line 84 / post.md line 22:** the dataset is described three different ways — the story says "three benchmarks" (AIME25, GPQA, BFCL) yet its own tables include a fourth, "Multi-turn base" (line 179); the report says "nine tests spanning four categories"; the post lists four loose dimensions — a fresh reader cannot map the same 320,192 rows across the three descriptions.
3. **post.md line 64 / story.md line 110 / report.md line 379:** the medium-effort "near 100%" plateau is given as "362 through 1k tokens" (post), "from 362 to 5,793 tokens" (story), and "from 256 through ~1k … 88–93% at 2k–4k" (report); the story's own table (line 102) shows 90.7% at 2,896 and 84.3% at 5,793, so the story's "near 100% … to 5,793" is contradicted by its own data while the post and report give a different range.
4. **story.md line 245 / report.md line 396:** the story says "On BFCL tasks, the pattern reverses slightly: high effort sometimes underperforms medium" immediately after its own pooled BFCL tables (lines 187-227) where high > medium with no reversal, while the report resolves the statement by fc mode (fc=1 slips at high, fc=0 keeps climbing) — but the story never mentions fc mode at all, so a reader moving story → report hits a direct contradiction about the same BFCL numbers.
5. **story.md lines 19-21 (with table headers from line 163) / plots.md line 307 / post.md line 11:** the story presents the effort curves as measured on "llama.cpp and vLLM … not an artifact of a particular inference engine," but its table headers say "burrito with llama.cpp/vLLM" and plots.md Fig. 6.15 says the matched-length analysis uses "burrito backends, both tool modes" — a reader coming from the post (which says the vanilla backends are broken, line 11) cannot tell whether the headline finding is about vanilla backends or the harness.
6. **post.md lines 50/78-80 / story.md lines 163-169 / plots.md line 271:** the post's "The tradeoff landscape" presents "35.5 percentage points" and "~2.2K/11.8K/39K tokens" as general model behavior, but the identical numbers are scoped in the story to "AIME25, burrito with llama.cpp, fc_model=0"; the post also places 59pp (line 50, matched budget) and 35.5pp (line 78, overall) only ~28 lines apart, both labeled AIME25, with no note that they are different measurements.
7. **report.md lines 124-127 / plots.md lines 135, 291:** the report lists "Python tool: enabled or disabled" as a configuration dimension independent of "Function calling mode: fc_model=0/1," but plots.md's Fig. 6.13 title says "fc1 → python tool enabled" and phase 4 shows python only on AIME25 for burrito — a fresh reader cannot build the design space from the report's "independent dimensions" table.
8. **report.md line 37 / plots.md throughout:** the report says "every chart in this report is part of the complete 44-figure record kept in plots.md," but the report's integer figure numbers do not resolve against plots.md's phase-numbered figures — report "Fig. 7" = plots "Fig. 5.1," report "Fig. 12" = plots "Fig. 4.1," report "Fig. 6"/"Fig. 6b" = plots "Fig. 5.7"/"5.8" (and Findings 1-3 cite Figs. 1-5, none of which exist in plots.md) — a reader following the cross-reference by number gets a different or nonexistent figure.
9. **plots.md lines 265 (Fig. 6.8) / 271 (Fig. 6.9) / report.md lines 453, 465:** Fig. 6.8 claims "Schema tools at medium (83.3% at 6,738 tokens) beat schema-free medium by 15 points," but its own note says the fc1 lines are AIME25-only while the fc0 lines are the AIME25+GPQA pool — an apples-to-oranges 15-point comparison; report.md then cites the same 83.3%/6,738 as if it were a clean AIME25 schema comparison, and a fresh reader cannot tell which scope the number really has.
10. **(internal to report.md) Finding 1, line 153 vs line 157:** the breakdown says 5,925 incorrect responses total, but the per-backend list that follows names only 4 of the 7 backends and sums to 5,131 (1,959 + 2,051 + 558 + 563) — the 794-response gap (burrito-pt and fixed-jinja backends) is never accounted for.
11. **(internal to story.md) line 73:** "GPQA questions require less reasoning than AIME25 problems, so the absolute accuracy is higher across the board" is contradicted by the story's own tables on both sides of it — at ~362 tokens AIME25 low is 78.1% vs GPQA low 55.7%, and AIME25 medium 100% vs GPQA medium 87.7%.
12. **post.md line 62 / story.md line 108 / report.md line 377:** the same low-effort AIME25 crash is given three endpoints — "crashes to 19% at 2,896 tokens" (post), "crashes to 0% as reasoning token count increases" (story), "collapses to 0% by the 8k–16k bin" (report) — each true at a different point on the curve with no indication of where (related drift: post line 66 "above about 88%" vs story line 112 "above 90%" for the high-effort floor).
13. **(internal to report.md) line 31 vs line 430 vs line 449:** the exec summary says "+21 points at low effort," Finding 7's table says "+21 to +24%," and the body says "21-24 percentage points" — a range vs a point, and "%" vs "points" used inconsistently in the same finding.
14. **report.md line 260 / plots.md line 173:** the same cell is "0.2%" (report Finding 4 table) vs "0.25%" (plots Fig. 5.1) for default-jinja multi-turn fc=0.
15. **(internal to report.md) line 74 vs line 106:** the "Evaluation Framework" paragraph names four backends while the "Backends" section lists seven — the fixed-jinja and burrito-pt variants appear only in the table, never in the framework text.
16. **(internal to story.md) line 19 vs line 291:** the same benchmark is "GPQA" (line 19) and "GPQA Diamond" (line 291); the report (line 74) uses plain "GPQA," so a fresh reader is left wondering whether they are different subsets.
17. **report.md line 74 / plots.md line 103 onward / report.md line 401:** the leaderboard is "Big Function Calling Leaderboard (BFCL)" in the report's benchmark definition, but "BFCL_v4" in the plots' phase titles and "Big Function Calling Leaderboard v4" in the story's Methodology (line 291) — the version number appears in the data documents but not in the report's definition.
18. **report.md lines 118/124 / plots.md phase titles (lines 103, 135, 167):** the report says "Burrito backends use /v1/completions for all backend communication," yet its configuration table splits all rows into "101,120 chat rows, 219,072 responses rows" and the plots' phases label burrito runs as "responses API" — a fresh reader cannot reconcile which wire the burrito rows are counted under.
19. **report.md line 453 / plots.md line 295:** the report attributes "about 6x fewer tokens" to the python toggle by comparing python-ON at *medium* effort (6,738 tokens) with python-OFF at *high* effort (38,971), but the clean same-effort comparison in plots Fig. 6.13 shows only about half the thinking — a fresh reader cannot tell how much of the 6× is python vs effort level.
20. **post.md lines 90-91 / report.md line 59:** the post's release list names two repos, "burrito-core" (the harness) and "burrito-evals" (the suite), while the harness itself is called just "burrito" in the post's title and "Burrito" throughout the report — a fresh reader cannot tell whether burrito-core, burrito, and burrito-evals are one thing, two things, or three.
21. **(internal to story.md) line 249:** "The reasoning-to-output token ratio shifts from 70% at low effort to 98% at high effort" cannot be reproduced from the story's own median-token tables under either backend (llama.cpp low is 1,362/2,169 ≈ 63% and falls to 57% at medium; vLLM low is 78%; only vLLM high reaches ~98%), so a careful reader recomputing from the tables two paragraphs above trips on the claim.

## 5. Missing ramps between the audience levels (post → story → report)

1. **post → story:** the post (line 22) promises "preserved thinking" as one of the four eval dimensions, but story.md — the deep-dive the post points to — never covers it and never points forward to report.md Finding 5, so a reader expecting all four named dimensions gets three with no signpost.
2. **post → story:** the audience jump is unmediated — the post writes in slangy first-person for a general reader, while the story opens as a neutral technical essay ("What 320,000 model runs reveal about the hidden architecture of test-time compute") with no recap of why gpt-oss's infrastructure was broken (the post's entire setup), no reminder that burrito is a harness, and no definition of what "reasoning effort" is as a model feature — a fresh reader jumping straight to the story (as post line 96 invites) has no context for why it matters.
3. **post (title) → report:** the post's headline "3.49B tokens" is never explained in the post body or the story; only report.md (line 129) mentions the number, without even saying what it counts (output tokens? input+output?) — the set's biggest headline number has no ramp at any level below the report.
4. **post → report:** the post's setup names three broken things — "The prompt templates were broken. The wire APIs were mismatched. The tool schemas were ignored." (line 11) — but the report's findings cover only the first two (Findings 2 and 3); "the tool schemas were ignored" has no counterpart finding (Finding 4 is about schemas *helping* when present, a different claim), leaving the post's third failure mode dangling.
5. **story → report:** the story's closing note (line 301) is the only cross-reference to the report, and it uses names the reader cannot map (see 4.1); the report never acknowledges the story's existence at all, so a story-reader arriving at the report gets no preview of Findings 1-5 and 7 or how they connect to the reasoning story.
6. **post → report:** the post's title promises "an inference harness that fixes gpt-oss" and post line 100 says the fixed jinja template is being PR'd to OpenAI's HuggingFace repo — but neither document answers the question this combination raises: if the fix upstreams into the official template, what exactly does burrito still fix that the fixed template does not?
7. **post → story:** post line 96 promises story.md has "all the data, tables, and analysis," but the story's tables cover only the burrito backends (see 4.5) and center on AIME25/GPQA, so a reader expecting the full 320,192-run picture (vanilla backends, all nine tests) gets a scoped subset without a note about the scoping.
