# Change list — post.md polish (EDITOR-POST run)

File: `burrito-evals/report/post.md` — 162 lines before, 152 lines after. Exactly two edits were made.

## Edit (a) — de-duplicate the Qwen section

- Byte-identity verification: extracted the first copy (orig L126–L132, heading through last content line) and the second copy (orig L136–L142) to separate files; `cmp` reported them byte-identical (no differences).
- Before: duplicated second copy at orig L136–L142, bracketed by a redundant `---` separator (orig L134–L135) and a trailing blank line (orig L143) — 10 lines total, orig L134–L143.
- After: second copy and its redundant separator/blank lines deleted (10 lines removed); first copy kept untouched at L126–L132; a clean single blank line now separates the Qwen section from the `---` → `## Credits` boundary (new L133–L136).
- One-line description: removed the byte-identical second "## Qwen can't slop my ass" block (heading + 3 paragraphs) plus its redundant `---` and blank lines, so one clean single-blank-line separation remains before Credits.

## Edit (b) — TPS wording

- Before (L120): "…It flies at 200-300 TPS (tokens per second) on consumer hardware."
- After (L120): "…It flies at 150-200 TPS (tokens per second) on consumer hardware."
- Occurrences: 1 (sole occurrence of the phrase in the file).
- Dash codepoint note: the edit spec described the dash as U+2013 (en dash), but the file actually contains an ASCII hyphen-minus U+002D (verified with `od -c`; `grep -c '200–300 TPS'` = 0, `grep -c '200-300 TPS'` = 1). Per the no-other-fixes policy, the existing dash byte was preserved exactly; only the digits changed (200→150, 300→200). The document's range style is hyphens throughout (e.g., "128-256", "8k-16k", "2k-4k"), so the preserved hyphen is also the consistent choice. No verification against eval_results.csv was performed, as instructed.

## Verification

- `diff` of the pre-edit backup against the edited file shows exactly two changes: the L120 digit change and the 10-line block deletion (diff aligned the removal to the first identical copy; the surviving content is the kept block — verified byte-for-byte, including the preserved trailing spaces on the two Qwen paragraph lines). No other line was touched; file tail intact (`*Edit: Links added. Repo is live.*` at final line).
