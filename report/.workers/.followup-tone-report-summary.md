# Tone/flow/readability summary — report.md (rung c)

Scope: applied the findings owned by report.md (the "Report.md-owned fixes" list in `.followup-tone-post-summary.md`) plus one drift correction against plots.md (Finding 4 table, 0.2% → 0.25%). Finding IDs below are the current `.followup-reader-findings.md` numbering; the post summary's parentheticals predate the final numbering. Rung-c premise: the reader has read post.md and story.md, but jargon is still defined at first use, and cross-doc inconsistencies are fixed on the report side. No claims or numbers beyond plots.md were added; section order, heading names (TOC anchors), and all non-touched prose are unchanged. Every touched number was re-verified against plots.md before and after.

## Changes (location — what — why)

**Executive Summary**
1. "Every chart…" paragraph — figure numbers now "follow plots.md's phase scheme (leading number = phase, e.g. Fig. 5.7 is phase 5, figure 7)" — 4.8: the report's old integer figure numbers did not resolve against plots.md's phase-numbered figures; the gloss makes the renumbered captions decodable without opening plots.
2. Bullet 7 — "+21-24 points at low effort, +10-12 points at medium, mixed at high" — 4.13: the file said "+21 points" (exec) vs "+21 to +24%" (table) vs "21-24 percentage points" (body); deltas are now consistently "points".
3. "The biggest story" — "the pattern holds across backends and benchmarks at very different accuracy levels" (was the unclear "translates across backends and resulting accuracies") — 3.5 (report instance).

**Background — The Problem**
4. llama.cpp paragraph — grammar constraints glossed: "decoding under a format grammar that forces each output to fit a declared tool-call structure" — 2.12: the mechanism was a black box that "biases model output".
5. llama.cpp paragraph — added that with no route for the model's native tool schemas they are "effectively ignored by the vanilla client: the post's third broken thing, 'the tool schemas were ignored.'" — 5.4: the post names three broken things; the report had a counterpart finding for only the first two, leaving the third dangling (no new finding invented — the sentence states the vanilla-client fact the post already names).

**Background — Evaluation Framework**
6. First paragraph — "multiple backends (four named)" → "the seven backend configurations tabulated in Experimental Setup" — 4.15: framework text (four) contradicted the seven-row table; fixed-jinja and the burrito-pt variants appeared only in the table.
7. Data paragraph — defined the unit of analysis: "one row per run, a run being one model attempt at one benchmark item under one configuration and one seed" — 1.8 (report-local part): runs/rows were never reconciled in any document.
8. New paragraph — defined the plots vocabulary the findings lean on: numbered phases (one per sweep), units (a single benchmark item — task, problem, or question), and figure columns (units arranged over the backend/tool/effort grid) — 2.10: Finding 1 used "phase", "column", and "units" with no introduction.

**Experimental Setup — Backends**
9. New paragraph — the phases behind the charts use different subsets of the seven: phase 1 excludes the preserved-thinking variants; Fig. 6.6 covers the five non-pt backends; Fig. 6.7 all seven; phase 5 is built around the burrito stacks — 1.7: the seven were presented as one fixed set, so a reader assumed all seven appeared in every table (e.g. Finding 5's two-row table).
10. New paragraph — the wire API re-explained as the client-facing endpoint; burrito always speaks `/v1/completions` to the backend; that is why burrito rows are counted under both wires in the tables — 4.18: the config table splits all rows into "101,120 chat / 219,072 responses" while the burrito bullet said completions-only, and plots' phases label burrito runs "responses API".
11. Configuration Dimensions, wire bullet — relabeled "Wire API (client-facing)" — 4.18: matches the re-explanation above.
12. Configuration Dimensions, python bullet — "Not a free axis: python-enabled runs are the burrito AIME25 runs with fc_model=1 (plots.md phase 4, Fig. 6.13)" — 4.7: the dimension was listed as independent of fc_model and backend, but the data shows it locked onto burrito × AIME25 × fc_model=1 (1,440 rows).

**Finding 1: The Data**
13. "the phase's outlier error column" → "the outlier of phase 1's per-task error breakdown" — 2.10: names the phase and the breakdown type.
14. Appended "the remaining 794 of the 5,925 sit in the preserved-thinking and fixed-jinja variants" — 4.10: the per-backend list (1,959 + 2,051 + 558 + 563 = 5,131) never accounted for the full 5,925; the arithmetic now closes.
15. "once the FC model is enabled" → "once fc_model=1 is enabled" — 2.11: a mode re-cast as a phantom model.
16. Captions — "Fig. 1" → "Fig. 1.1"; "Fig. 2" → "Fig. 1.5" — 4.8: integer numbers that do not exist in plots' phase scheme.

**Finding 3**
17. Captions — "Fig. 3" → "Fig. 1.1", "Fig. 4" → "Fig. 1.5", each marked "(same chart as in Finding 1, shown for the multi-turn region)" — 4.8 + 3.7: the same image files were embedded under different numbers with different captions; the repeated charts now self-identify as the same chart shown for a specific region.

**Finding 4**
18. Accuracy table, "llama.cpp, default jinja" fc=0 — 0.2% → 0.25% — drift correction against plots.md (Fig. 5.1's responses-wire value; Finding 4's table holds phase-5/responses-wire values). Finding 2's 0.2% (chat wire) left untouched — a different measurement, not a contradiction. [Current findings file 4.14.]
19. Collapse sentence — same 0.2% → 0.25% correction in prose.
20. "Failures Concentrate at Turn 0" — one dense paragraph split into three labeled bullets: (a) vanilla AST-parsed backends die almost at once (vLLM 0.6%, default jinja 2.0%, 639 of 1,600 trajectories even reach turn 0 — "trajectory" glossed at first use), (b) per-turn failures compound end-to-end (0.9–7.1% alive through turn 6 with schema tools), (c) turn 0 drives the headline — 3.9: three different measurements packed into one block; the thread is now explicit.
21. Captions — "Fig. 5" → "Fig. 1.1" (marked "same chart as in Findings 1 and 3, shown for the multi-turn region"), "Fig. 6" → "Fig. 5.7", "Fig. 6b" → "Fig. 5.8" — 4.8 + 3.7.

**Finding 5**
22. Caption — "Fig. 7" → "Fig. 5.1" — 4.8.

**Finding 6**
23. Fig. 6.8 caption — appended "(GPQA ran fc_model=0 only, so the fc_model=1 lines are AIME25 alone.)" — 4.9 (report-side mitigation): the fc1 numbers were readable as a clean AIME25 schema comparison; the scope fact comes from plots.md. The full caveat is deferred to plots.md (see below).
24. "What This Means" — "translates across backends and resulting accuracies" → "the same curves appear on llama.cpp and vLLM, and across AIME25, GPQA, multi-turn, and BFCL tests, at very different accuracy levels" — 3.5 (the verbatim phrase is unclear English; now says what holds and where).

**Finding 7**
25. Accuracy table — deltas in points: "+21 to +24 points", "+10 to +12 points", "Mixed (+2.5 points llama, −5.8 points vllm)" — 4.13 (matches plots' values, e.g. +21.25 at low, −5.83/+2.50 at high).
26. Token Reduction — re-anchored to the same-effort comparison: "at the same (medium) effort the python-enabled AIME25 runs think about half as much as the no-python runs (plots.md Fig. 6.13)" — 4.19: the old text attributed "about 6x fewer tokens" to the python toggle by comparing python-ON at medium (6,738 tokens) with python-OFF at high (38,971); a reader could not separate python's effect from effort's.
27. "What This Means" — the cost discussion split into two labeled views: same-effort (about half the thinking, Fig. 6.13) and cross-effort (83.3% at 6,738 tokens ≈ 83.8% at 38,971, "about a fifth of the tokens"); the burrito@vllm pair (85.8% vs 87.5%) kept verbatim with de-scoped wording ("the accuracy pattern is less clear") because the pairing could not be fully resolved to effort levels from plots' text — 4.19: no new claims, and no effort-level assertion that plots does not support.
28. Caption — "Fig. 12" → "Fig. 4.1" — 4.8.

**Conclusion**
29. Python bullet — "adds 21-24 points of accuracy at low effort and 10-12 points at medium effort" — 4.13: consistent with the exec summary and Finding 7 table.

## Deferred to plots.md

- **1.8 (global unit vocabulary — evals / runs / rows / units / tasks / problems / trajectories):** the report now defines run/row/phase/unit/column locally for its own readers (Evaluation Framework), and the post now ties rows↔runs internally (post subagent), but the full fix — defining how the plots' caption vocabulary (units, tasks, problems, trajectories) relates to runs/rows — can only live in plots.md.
- **2.5 (reasoning tokens vs output tokens):** the post now defines the pair for its own readers; a shared definition has no home yet — plots.md is the natural one (per the post subagent's deferral). The report relies on the post's definition for rung-c readers and defines no new claim of its own.
- **3.8 (stray title):** plots.md opens with the post's title verbatim before "## Abstract"; a reader opening plots.md cannot tell what document they have. The fix belongs in plots.md.
- **4.9 (Fig. 6.8 apples-to-oranges, full caveat):** per the post subagent, the figure compares per-seed pass^8 at matched reasoning length (y) against pooled accuracy across 8 seeds (x), and its fc1 lines are AIME25-only while the fc0 lines are the AIME25+GPQA pool. The report's caption now carries the scope fact it can carry (item 23), but the caveat itself — and any corrected "15 points" statement — belongs in the plots.md figure note.
- **5.3 (composition of 3.49B tokens):** no document states whether the headline token count is input, output, or both; the post now claims only "total across all runs". The plots.md abstract — the source of truth — must state the composition; the report (which quotes the number) cannot assert it without a new claim.

## Not addressed (and why)

**Post.md-owned** (handled by the post subagent; the report side has nothing to change): 1.1 (batch size 1), 1.2 (8 seeds), 1.3 (TPS — acronym expanded, number left as-is as single-sourced), 1.4 (.gguf filename), 2.1 (wire APIs), 2.2 (fc=1), 2.3 (preserved thinking), 2.4 (test-time compute), 2.5 post part, 2.6 (AIME25/GPQA expansion), 3.1 (duplicated Qwen section — left as-is on both copies per explicit instruction), 3.2 (narrator switch), 3.4 post side (binning method), 4.6 (tradeoff-landscape scoping), 4.20 (burrito-core/burrito-evals — the report consistently uses "Burrito" for the harness), 5.6 (what burrito still fixes vs the upstream PR — answered in the post), 5.7 post side (story bullet scoped).

**Story.md-owned** (not this file): 1.5 (producer/temperature), 1.6 (320K runs vs 300K questions), 2.7 (fc_model undefined in the story), 2.8 (multi-turn "tasks"), 2.9 (MXFP4/GGUF/safetensors in the methodology), 3.3 ("this model" on the thesis sentence), 3.5 story instance (the report instance is fixed above), 3.6 (unexplained "--" cells vs the all-three-efforts inclusion rule), 4.2 (the story says "three benchmarks" yet its own tables include multi-turn — the report's "nine tests spanning four categories" matches plots' nine tests, so the story wording is the fix), 4.3 (the story's "near 100% … to 5,793" contradicts its own table — 84.3% at 5,793; the report's "256 through ~1k, 88–93% at 2k–4k" is consistent with its own table and with plots Fig. 6.1), 4.4 (the story's BFCL-reversal claim without fc mode — the report already resolves it by fc mode: fc=1 slips at high, fc=0 keeps climbing), 4.5 story side (vanilla-vs-harness ambiguity in the story's framing — the report scopes the matched-length analysis to burrito backends in its Fig. 6.15/6.16 captions; the post side was fixed by the post subagent), 4.12 story side (the report's endpoint is already located: "collapses to 0% by the 8k–16k bin"; the post side was fixed by the post subagent), 4.11 (internal to story: "GPQA requires less reasoning … higher across the board" contradicted by the story's own tables — the report makes no such claim), 4.16 (internal to story: "GPQA" vs "GPQA Diamond" — owned by report.md per the post summary, but no report change is needed on two grounds: the report uses "GPQA" exactly as plots.md does, which never says "Diamond", and the report's 83.8% is the standard 1-decimal rounding of plots' 83.75%), 4.21 (internal to story: the 70%→98% reasoning-to-output ratio is not reproducible from the story's own tables — the report makes no such claim), 5.1 (the story never points to report Finding 5 for preserved thinking), 5.2 (unmediated audience jump; no recap intro for the post-reader), 5.5 (the story's closing note is the only cross-reference to the report; the report stays self-contained at rung c and deliberately adds no back-pointer), 5.7 story side (no scoping note in the story itself that its tables cover only the burrito backends).

**Cross-document, report side already fine:** 4.1 (finding names — the report's seven titles already map one-to-one onto the post's seven finding names; the post subagent added the mapping in the post's report bullet; renaming report headings is unnecessary and would break the TOC anchors) and 4.17 (BFCL vs BFCL_v4 — the report deliberately keeps both forms: the reader-facing "Big Function Calling Leaderboard (BFCL)" in the benchmark definition and plots' "BFCL_v4" in the plot-derived Fig. 6.4 caption; version unification is a plots/story-side matter).

## Repair pass (E.3 tone gate — 1 issue)

- `report.md` L161 (Finding 1, "The Data", the edit-14 appended clause) — "the remaining 794 of the 5,925 sit in the preserved-thinking and fixed-jinja variants" → "the remaining 794 of the 5,925 sit in the fixed-jinja variant." The irrelevance test exists only in phase 1 (which explicitly excludes burrito-pt) and phase 3 (the two non-pt burrito backends, twelve bars per task), so the preserved-thinking variants have no irrelevance runs at all and the 794 belongs entirely to the fifth non-pt backend, llamacpp@fixed-jinja — the only one of the seven not yet named in that enumeration. Arithmetic unchanged (1,959 + 2,051 + 558 + 563 = 5,131; 5,925 − 5,131 = 794); no new claims or numbers beyond plots.md, and no other text in either file was touched.
