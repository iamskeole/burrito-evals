# Tone/flow/readability summary — post.md (rung a)

Scope: only .followup-reader-findings.md entries applicable to post.md were addressed; every number, model name, and claim in the file was re-verified against plots.md before and after editing (none changed). The duplicated "Qwen can't slop my ass" sections (×2) were left exactly as-is, per instructions.

## Changes (location — what — why)

1. "The story" — named OpenAI as the producer of gpt-oss-20b (rung a: the producer was otherwise only named in the PR paragraph near the end).
2. "The story" — glossed "wire APIs" as the protocol a client uses to talk to the inference backend — finding 2.1 (jargon used before introduction).
3. "The story" — added short inline glosses at first use: preserved thinking (thinking kept across turns instead of pruned), effort levels (low/medium/high "how hard to think"), 8 seeds (8 runs per configuration with different random seeds — signal vs luck), batch size 1 (one request at a time), MXFP4 (a 4-bit format) — findings 1.1, 1.2, 2.3, 2.9 (post part).
4. "The story" — glossed "fc=1" (fc = function calling; 1 = the mode with structured tool schemas) — finding 2.2.
5. "The story" — defined "reasoning tokens" vs "output tokens" (output splits into thinking + visible answer; output tokens = the whole) — finding 2.5 (post part).
6. Finding 1 — expanded AIME25 at first use (2025 American Invitational Mathematics Examination) and added a method note that token counts are grouped into doubling bins (128-256, 256-512, 1024-2048, ...) labeled at their midpoint, so "~1,448" means the 1024-2048 bin — findings 2.6, 3.4 (bin values appeared with no binning method).
7. Finding 1 — expanded GPQA at first use (graduate-level science questions) and attributed "nearly identical curves" to burrito's llama.cpp and vLLM backends, matching the chart's actual scope (burrito backends, both tool modes, per plots.md Fig. 6.15) so a reader who just read "the vanilla backends were broken" can tell which backends produced the headline finding — findings 2.6, 5.5 (post side).
8. Finding 2 — extended the low-effort crash from "19% at 2,896" to "and to 0% by the 8k-16k bin" (matches plots.md Fig. 6.1: "collapses to 0% by 8k–16k") so the crash has a located endpoint instead of a dangling 19% — finding 4.12 (post side).
9. Finding 2 — glossed "test-time compute" (the thinking the model does at generation time) — finding 2.4.
10. "The tradeoff landscape" — added a scope note: the 35.5pp / 10pt / ~2.2K / ~11.8K / ~39K numbers are AIME25 on the burrito llama.cpp backend without schema tools (fc=0), and they are a different measurement from Finding 1's 59 points (there the token budget was held fixed; here effort changes both budget and strategy) — finding 4.6.
11. "The tradeoff landscape" — small "Pareto front" gloss (best accuracy-per-token deal) — rung a readability.
12. "What we are releasing" — burrito-core bullet identifies it as the "burrito" of the title; burrito-evals bullet says it is the complete suite (every backend, every scenario, every finding) — finding 4.20 (one thing vs two vs three).
13. "What we are releasing" — eval_results.csv bullet ties rows↔runs: one row per run, a run = one model attempt at one benchmark item under one configuration and one seed — finding 1.8 (post-internal part; "evals"/"runs"/"rows" are now reconciled within the post).
14. "What we are releasing" — report.md bullet maps the post's finding names to the report's actual titles ("the reasoning story" = "Not All Reasoning is Created Equal", "native tool utility" = "Python Tool Impact") so a post→report reader can locate the sections — finding 4.1.
15. "What we are releasing" — story.md bullet scoped to what the story actually contains (AIME25, GPQA, multi-turn, and BFCL runs on the burrito backends) and expanded BFCL at its first appearance in the post — finding 5.7 (post side).
16. "What we are releasing" — after the template-PR sentence, stated what burrito still fixes that the PR does not (the wire protocol to the backend and the tool-call parsing; the template fix alone puts vanilla llama.cpp at parity with burrito on tool-free reasoning benchmarks, per plots.md) — finding 5.6.
17. "The worker bee" — expanded TPS = tokens per second (number left exactly as-is) — finding 1.3.
18. "The worker bee" — explained the .gguf co-author filename (27B Qwen 3.6 model, quantized to 4-bit by unsloth, running locally) — finding 1.4.
19. "Credits" — explained the title's 3.49B tokens as the total token count across all 320,192 runs (consistent with plots.md; composition intentionally not asserted — see Deferred) — finding 5.3 (partial).
20. "Credits" — added a cue that the Qwen co-author writes the next two sentences, making the narrator switch from the human author explicit — finding 3.2.

## Deferred to plots.md

- 1.8 (global unit vocabulary): the post now ties rows↔runs internally, but the full fix — defining how "units" (plots captions, e.g. line 69), "tasks", "problems", and "trajectories" (Fig. 5.7) relate to "runs"/"rows"/"evals" — can only live in plots.md.
- 3.8: plots.md opens with the post's title before "## Abstract"; the stray-title fix belongs in plots.md.
- 4.9: Fig. 6.8 compares per-seed pass^8 at matched reasoning length (y) against pooled accuracy across 8 seeds (x); the apples-to-oranges caveat belongs in the plots.md figure caption (report.md, which cites it, is also affected).
- 5.3 (composition of 3.49B): no document states whether it counts input, output, or both; the post now only claims it is the total across all runs. The source of truth (plots.md abstract) should state the composition.
- 2.5 (global definition): the post now defines reasoning vs output tokens for its own readers; story.md/report.md/plots.md still never define them, and plots.md is the natural home for the shared definition.

## Not addressed (and why)

- 3.1 (duplicated "Qwen can't slop my ass" section): left exactly as-is on both copies per explicit task instruction (documented pre-existing editorial duplication).
- 1.3 ("200-300 TPS" value): the acronym is now expanded, but the number itself is single-sourced (appears only in post.md) and cannot be checked against any other document; kept unchanged.
- Story.md-owned fixes: 1.5 (producer unnamed), 1.6 (320K runs vs 300K questions never reconciled), 2.7 (fc_model undefined), 2.8 (multi-turn "tasks" undefined), 2.9 (MXFP4/GGUF/safetensors in methodology), 3.3 ("this model" ambiguity), 3.5 ("translates across backends and resulting accuracies"), 3.6 ("--" cells vs bins with all three efforts), 4.2 (three conflicting dataset descriptions), 4.3 (medium-effort plateau "to 5,793" contradicts its own table; the post's "362 through 1k" is the version consistent with plots.md Fig. 6.1, so the story side is the fix), 4.4 (BFCL inversion without fc mode), 4.5 story side (vanilla-vs-harness ambiguity in the story's framing), 4.12 story side, 4.14 (0% vs 0.1% mismatch), 4.17 (BFCL row inconsistency), 4.21 (pass^8 vs ratio confusion), 5.1 story side (no pointer to report Finding 5 for preserved thinking), 5.2 (no recap intro for the post-reader), 5.5 story side, 5.7 story side (no scoping note in the story itself).
- Report.md-owned fixes: 1.7 (seven backends presented as one fixed set), 2.10 (phase/column/units undefined), 2.11 ("the FC model" undefined), 2.12 ("grammar constraints" undefined), 3.7 (duplicate "Fig. 6.15" numbering), 3.9 (Finding 4 dense paragraph), 4.7 (irrelevance paradox not scoped to f1), 4.8 (f3/f4 scope ambiguity), 4.10 (794-response gap between f04 and f07), 4.11 (f07 scope vs report text), 4.13 (Finding 6 missing fc-mode split), 4.15 (26.3% vs 30.2% at 46,341), 4.16 (83.8% vs 83.75%), 4.18 (completions-wire vs "responses API" label contradiction), 4.19 (pass^8 77.1/83.8 mismatch), 5.4 (post's "tool schemas were ignored" has no matching report finding).
