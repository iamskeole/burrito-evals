# story.md — polish edits (bounded scope, L280–L309)

## Edit 1 — L295
- Before: `vLLM (safetensors — PyTorch's file format for model weights — in the same MXFP4 quantization)`
- After: `vLLM (safetensors — Hugging Face's file format for PyTorch tensors — in the same MXFP4 quantization)`
- Note: attribution corrected (safetensors is Hugging Face's format for PyTorch tensors, not "PyTorch's file format"); surrounding parenthetical structure and sentence kept intact.

## Edit 2 — L309
- Before: `...a broader evaluation covering 320K runs, 300K questions, and 1,062 GPU hours of compute. A run is one question × seed × configuration — ... — so the two headline counts are two views of the same data: 300K questions (the benchmark items) and 320K runs (the individual question × seed × configuration attempts that evaluate them).`
- After: `...a broader evaluation covering 320K evaluations, 300K units (benchmark items), and 1,062 GPU hours of compute. An evaluation is one question × seed × configuration — ... — so the two headline counts are two views of the same data: 300K units (benchmark items) and 320K evaluations (the individual question × seed × configuration attempts that evaluate them).`
- Note: aligned with plots vocabulary (human-approved: 320,192 = evaluations) — "320K runs" → "320K evaluations", "300K questions" → "300K units (benchmark items)", and the defined term "A run is" → "An evaluation is" so the definition matches the headline counts; ~300K figure kept, no new claims or numbers.

## Scope
- Only lines 295 and 309 (within owned region L280–L309) were touched; file remains 309 lines; all other lines byte-identical to baseline (exact-string replacements, no reformatting).
