# VERIFY-POST verdict — post.md (rung a) vs plots.md (source of truth)

Audit of CURRENT on-disk post.md (160 lines, 10,589 B). 4068f9a6's diff vs the pass-1 before-snapshot = 6 hunks / 7 changed lines (L46, L47, L50, L62, L64, L66, L80); current file is byte-identical to the 3rd-manager baseline, so no writer has touched it since. All 7 changes verified correct against plots.md (38.3/97.1/100.0 @ ~1.4k per plots L29/L307; 59pp = 97.1−38.3; 100% in 128–256 bin per plots L223; 88–93% @2k–4k and ~88%→32k–64k per L223; 44% within the ~38–44% right-tail range per L307; 2,169/11,816/38,971 per L271). Zero stale or contradictory values remain. plots.md, story.md, report.md are byte-identical to their baselines (containment clean).

## Remaining issues

1. **post.md L40, L60, L76 — broken image references (pre-existing; the only fixable defect).** All three use the prefix `../burrito-evals/plots/`, which from `burrito-evals/report/` resolves to `burrito-evals/burrito-evals/plots/` — does not exist on disk (verified). All three sibling docs in the same directory (story.md, report.md, plots.md) use `../plots/`, which resolves, and all three referenced basenames exist in `burrito-evals/plots/` (f15-pooled, f01-non_bfcl, f09-aime25). The prefix traces to the original PLAN.md convention (L145/L198, which assumed the final report would sit alongside `burrito-evals/`, not inside it). It predates 4068f9a6 (present in the before-snapshot; none of its 7 hunks touched image lines) and matches the exact defect class the manager already fixed in report.md (Step A) and story.md (9 paths). Fix: replace `../burrito-evals/plots/` → `../plots/` on those 3 lines only.

2. **Unsourced-but-legitimate-at-rung-a content — NO fix action; carry to final EXCEPTIONS (same disposition the manager gave story.md's ~44 items, gated PASS).** Independent inventory of everything not sourced in plots.md:
   - L9: "August 2025" release, "20 billion parameters, open weights", "community was excited" (external model history).
   - L13: "something like GPT-4" (rhetorical analogy; no GPT-4 comparison exists in the compendium).
   - L15–16: Carl Sagan quote (external quotation).
   - L20 + L146: u/skeole burrito-delivery gig anecdote (personal narrative).
   - L22: "Temperature 1.0. MXFP4 quantization over 131K context." (setup details; zero hits in plots.md — same EXCEPTIONS class as story.md L19).
   - L24: "May 2026 backend versions" (not in plots.md, but repo-corroborated: `eval_versions.txt` FAKETIME=2026-05-25, vllm v0.21.0, llama.cpp 3a479c913) and "backends have improved since launch, especially on the fc=1 path" (temporal framing; unverifiable against the compendium).
   - L42/L62/L64/L66: exact bin values finer than the compendium's bin-level statements — 1,448 (≈ the cited "~1.4k"), 181 (in 128–256 bin), 2,896+19% (2k–4k bin, before the 8k–16k collapse), 362 (256–512 bin), 46,341+30% and 92,682+0% (32k–64k / 64k–128k bins; medium-effort tail not numerically stated in plots.md). Every value is consistent with a source-stated bin/range — none contradicts plots.md (shape-verified, per the region-B precedent).
   - L88: "MIT license" (not in plots.md, but TRUE in the repo: `LICENSE` = MIT, © 2026 iamskeole) — legitimate release statement.
   - L93: "Excel file used to programmatically compose eval commands" (repo-true: `artifacts/eval_specs.xlsx`); release description.
   - L100: "submitted as a PR to OpenAI's HuggingFace repo" (external action; not in compendium; legitimate release statement).
   - L118: "200-300 TPS" (zero TPS hits in plots.md — EXCEPTIONS-class performance claim) plus rhetorical "eager, accurate, and self-healing".
   - L120: "I spent a year", co-author identity "unsloth--Qwen3.6-27B-UD-Q4_K_XL.gguf" (personal/credits).
   - L124–140: "Qwen is a shit writer", "Gemma owns the prose game", "Phil Schiller said Apple can't innovate my ass", "100% locally generated" (opinion/external; see Duplication section).
   - L148–152: credits — "report notebook", "written by Grok 4.5", "OpenAI and Anthropic capped you after five posts", "No cloud credits" (process/external).

   Grouping: 14 fine-grained clusters above (they collapse to the claimed ~9 under coarse clustering). All are legitimate rung-a content — a "first view on r/localllama — assumes nothing" post legitimately carries voice, anecdotes, credits, release statements, and true external context — or EXCEPTIONS-class setup details. **No not-in-source group remains that is stale, contradictory, or data-claiming-against-source; the 9-groups-claimed-handled disposition (fix what plots.md supports, flag the rest, leave legitimate content in place) is confirmed.**

3. **Minor wording note (no action needed):** L80 "Token count increases exponentially" — the actual growth is 5.4× then 3.3× (2,169→11,816→38,971), i.e. a *decreasing* ratio; "exponentially" is colloquial for "by a lot". L82's "High effort reaches higher absolute accuracy" is the fc_model=0 AIME25 reading (73.8→83.8) of the two-curve f09 figure — the post consistently cites the fc0 numbers the caption leads with (35.5pp / +10pp), so it is a fair summary, though the fc1 curve in that same figure peaks at medium (83.3 vs 74.3). Neither contradicts plots.md.

## Duplication characterization

Block: the section headed **"## Qwen can't slop my ass"** + its two paragraphs (identifying fragment: "People love to say LLMs are slop machines. They say Qwen is a shit writer and Gemma owns the prose game.") appears **twice, verbatim**: post.md **L124–130** and **L134–140**, separated by the `---` rules at L132 and L142.
- **Pre-existing:** yes — the before-snapshot (pre-4068f9a6) contains the identical double section (4 occurrences of "Qwen can't slop" in both versions); none of the 7 changes touched that region.
- **Sourced in plots.md:** no. The block is opinion/external reference ("Phil Schiller", "Gemma", "100% locally generated", "zero cloud credits"); the only sourced atoms are the hardware facts "single RTX 3090"/"One GPU" (plots.md L1/L5). Legitimate rung-a voice, but not compendium-sourced.
- **Stale:** neither copy — they are byte-identical, so no divergence between them, and nothing in the block contradicts plots.md ("100% locally generated" is consistent with the credits section: prose co-authored by a locally-run Qwen3.6-27B GGUF, while Grok 4.5 wrote the analysis *scripts*, not the prose). Deletion/dedup of the second copy is a style call for the manager/fixer, not a factual correction.

## Verdict: ISSUES

One defect class remains: 3 broken image-reference prefixes (L40/L60/L76, `../burrito-evals/plots/` → should be `../plots/`, pre-existing). Bounded 3-line fix, identical to the manager's prior story.md/report.md path fixes. All 7 of 4068f9a6's changes verified correct; zero stale data; remaining unsourced content is legitimate rung-a / EXCEPTIONS-class (list above) and needs no edit.
